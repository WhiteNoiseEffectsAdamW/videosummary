{
  "name": "videosummary-backend",
  "version": "1.0.0",
  "description": "YouTube video summarizer — backend",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "node scripts/install-yt-dlp.js && cd client && npm install && npm run build",
    "postinstall": "node scripts/patch-deps.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "knex": "^3.1.0",
    "sqlite3": "^5.1.7",
    "youtube-transcript": "^1.2.1"
  }
}
